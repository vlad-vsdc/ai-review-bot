{
  "name": "@ai-review-bot/worker",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/worker.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@ai-review-bot/core": "*",
    "@anthropic-ai/sdk": "^0.125.0",
    "bullmq": "^6.3.6",
    "dotenv": "^16.6.1",
    "openai": "^7.15.0",
    "p-limit": "^7.3.2"
  }
}
